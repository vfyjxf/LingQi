# Continue Here — LingQi 前端 UI 合并

**创建时间：** 2026-05-30

## 当前状态

正在将 `D:\Downloads\ai-pr-review-assistant (2)` 项目的精良 UI 合并到 LingQi 项目。

**分支：** `feature/review-page-demo`
**远程 PR：** https://github.com/vfyjxf/LingQi/pull/12

## 已完成

- [x] 分析外部项目全部源码（App.tsx、PRReviewSummary、PRReviewStats、PRReviewRisks、types.ts、index.css）
- [x] 安装 `lucide-react` 图标库
- [x] 确定合并清单

## 待做（按顺序）

1. **重写 `app/page.tsx`**：引入外部项目的布局
   - 粘性顶栏（logo + CodeScope + "AI" 徽章）
   - Hero 区：模式选择器卡片（全息总览/安全强化/效能吞吐/逻辑漏洞）+ 示例 PR 快捷按钮
   - 加载区：5 步分级进度条（替换简单 spinner）
   - 错误区：红色左边框 + 图标 + 建议 + GEMINI_API_KEY 提示
   - 结果区：tab 切换（"效能与统计" / "核心隐患审计"）

2. **重写 `components/StatsPanel.tsx`**：
   - 评分徽章（A-F 圆形，0-100 分数）
   - SVG 环形图（severity donut chart，可点击筛选）
   - 横向柱状图（category bars，可点击筛选）
   - Props 扩展：`activeFilter` + `onFilterChange`

3. **增强 `components/RiskCard.tsx`**：
   - 默认展开（不是折叠）
   - lucide-react 图标替换文字（Shield/AlertTriangle/Cpu 等）
   - 文件路径徽章样式对齐外部项目
   - 点击展开/折叠箭头

4. **新建 `components/ReviewSummary.tsx`**：
   - Meta 头部（PR #、作者、文件数/+行/-行）
   - 2x3 网格：描述卡片（col-span-2）+ impact 卡片
   - 通用建议列表（编号圆形图标）

5. **类型对齐**：`lib/report/schema.ts` 的类型与外部项目 `types.ts` 的映射

## 关键决策

- 保留 LingQi 的 `slate-950` 配色（不切换为外部项目的 `#0d1117`），保持和已合入 PR 的一致性
- 保留 LingQi 已有的 `Severity`(blocker/major/minor/nit) 和 `RiskCategory` 类型，不改为外部项目的 high/medium/low
- lucide-react 已安装，直接可用

## 未提交文件

- `package.json`（新增 lucide-react 依赖）
- `pnpm-lock.yaml`（依赖锁定）

## 恢复第一步

```bash
git checkout feature/review-page-demo
pnpm dev  # 启动开发服务器
```

然后从 **待做第 1 项**（重写 `app/page.tsx`）开始。
