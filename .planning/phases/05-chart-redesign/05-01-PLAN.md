---
phase: 05-chart-redesign
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - components/StatsPanel.tsx
  - tests/stats-panel.test.tsx
autonomous: true
requirements: [chart-redesign]

must_haves:
  truths:
    - "User can see radial bar chart (rose chart) showing severity distribution with 4 colored segments matching the existing color scheme"
    - "Center of radial chart displays total risk count and label '风险项' via an absolute-positioned overlay div"
    - "User can click a radial bar segment to filter risks by that severity level"
    - "User can see radar chart showing category distribution across 4 axes (安全漏洞/性能瓶颈/编码逻辑/代码规范)"
    - "User can click a radar chart vertex to filter risks by that category"
    - "Clicking an already-active filter clears the filter (toggle behavior)"
    - "When riskCount is 0, both chart areas display the existing green empty state instead of charts"
    - "Grade badge column (leftmost in 3-column grid) is completely unchanged"
  artifacts:
    - path: "package.json"
      provides: "recharts dependency declaration"
      contains: "recharts"
    - path: "components/StatsPanel.tsx"
      provides: "StatsPanel component with Recharts RadialBarChart and RadarChart"
      contains: "RadialBarChart"
    - path: "components/StatsPanel.tsx"
      provides: "StatsPanel component imports"
      contains: "from \"recharts\""
    - path: "tests/stats-panel.test.tsx"
      provides: "Updated test suite verifying Recharts rendering"
      contains: "recharts-surface"
  key_links:
    - from: "RadialBarChart onClick handler"
      to: "onFilterChange('severity', key)"
      via: "Recharts onClick prop forwarding to parent callback"
      pattern: "onFilterChange.*severity"
    - from: "RadarChart Dot onClick handler"
      to: "onFilterChange('category', key)"
      via: "Recharts activeDot or custom dot onClick forwarding to parent callback"
      pattern: "onFilterChange.*category"
    - from: "Active filter toggle"
      to: "onFilterChange('clear', null)"
      via: "Conditional check in onClick: if already active, call clear"
      pattern: "onFilterChange.*clear"
    - from: "Recharts ResponsiveContainer"
      to: "responsive chart width"
      via: "Width/height props on ResponsiveContainer wrapper"
      pattern: "ResponsiveContainer"
---

<objective>
Replace the hand-rolled SVG donut chart (severity distribution) and horizontal progress bars (category distribution) in StatsPanel with Recharts RadialBarChart (nightingale rose chart) and RadarChart (radar/spider chart).

Purpose: Enhance visual impact for competition demo by using richer chart types that convey an extra dimension of information (radial radius = severity weight, radar polygon shape = risk profile silhouette).
Output: StatsPanel.tsx with Recharts-based charts, package.json with recharts dependency, updated test suite, all existing types and filter interfaces preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-chart-redesign/05-CONTEXT.md
@.planning/specs/2026-05-31-chart-redesign.md
@components/StatsPanel.tsx

# Source of truth for types, colors, filter interface, and grade badge implementation
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install recharts + replace severity SVG donut with RadialBarChart</name>
  <files>package.json, components/StatsPanel.tsx</files>

  <read_first>
  - components/StatsPanel.tsx — understand current donut SVG implementation, grade badge column (must preserve), types, colors, and filter logic
  - package.json — verify current dependencies list
  </read_first>

  <action>
  **Step 1a: Install dependency**
  Run `pnpm add recharts` at the project root.

  **Step 1b: Add Recharts imports to StatsPanel.tsx**
  Import from "recharts" the following named exports: `RadialBarChart`, `RadialBar`, `Cell`, `ResponsiveContainer`, `Tooltip`.

  **Step 1c: Build severity data for RadialBarChart**
  Create a data array `severityRadialData` from the existing `severitySegments` and `sevCounts`:
  - Each object has fields: `name` (Chinese label like "阻断"), `value` (count), `fill` (hex color), `key` (severity key like "blocker").
  - Map over `severitySegments` and use `sevCounts[seg.key]` for the value.
  - Colors: `#dc2626` (blocker), `#ea580c` (major), `#2563eb` (minor), `#6b7280` (nit).

  **Step 1d: Replace the severity donut section (middle column)**
  Keep the outer `<div>` wrapper with `rounded-lg border border-[#30363d] bg-[#161b22] p-6 shadow-sm` and the section header div (title "风险严重度统计" + 清除筛选 button + zero-risk branch).

  For the `hasRisks` branch (replaces lines 122-171):
  - Wrap in a `<div className="relative flex items-center justify-center">` with explicit height of `h-[280px]`.
  - Render `<ResponsiveContainer width="100%" height="100%">` wrapping a `<RadialBarChart>`.
  - `<RadialBarChart>` props: `cx="50%" cy="50%" innerRadius="30%" outerRadius="90%" barSize={14} startAngle={90} endAngle={-270} data={severityRadialData}`.
  - Inside the RadialBarChart, render:
    - `<RadialBar dataKey="value" background={{ fill: "#21262d" }} cursor="pointer">` with `onClick` handler that checks if `activeFilter?.type === "severity" && activeFilter?.value === entry.payload.key`, then calls `onFilterChange?.("clear", null)`, otherwise calls `onFilterChange?.("severity", entry.payload.key)`.
    - Inside `<RadialBar>`, map over `severityRadialData` to render `<Cell key={seg.key} fill={seg.fill} />` for each segment.
    - For active filter highlighting: set `opacity` on each `<Cell>` — when `activeFilter?.type === "severity"` and `activeFilter?.value !== seg.key`, set `opacity={0.35}`. When matching, set `opacity={1}`.
    - `<Tooltip>` with formatter that computes percentage: `(value / total) * 100` rounded to 1 decimal, returning `"{value} 项 ({pct}%)"` as the formatted string.
  - **Center overlay**: After the `</ResponsiveContainer>` (as a sibling, not a child), render an absolutely-positioned `<div>` with `pointer-events-none font-mono` classes inside `absolute inset-0 flex flex-col items-center justify-center`, containing the risk count in `text-lg font-black text-[#c9d1d9]` and the label "风险项" in `text-xs font-semibold uppercase tracking-widest text-[#8b949e]`.

  - Remove the donut calculation logic (`radius`, `circumference`, `donutSegments` variables at lines 75-84) since they are no longer needed.
  - Remove the legend sidebar div (the `w-28 space-y-2 text-xs` div at lines 149-170) since the Recharts Tooltip now provides the same information on hover.

  **Step 1e: Keep grade badge column completely unchanged**
  The entire first `<div>` in the grid (lines 92-104, with Award icon, grade circle, label, motto) must not be modified.
  </action>

  <acceptance_criteria>
  - `grep -c '"recharts"' package.json` returns a count >= 1
  - `grep -c 'from "recharts"' components/StatsPanel.tsx` returns a count >= 1
  - `grep -c 'RadialBarChart' components/StatsPanel.tsx` returns a count >= 1
  - `grep -c 'RadialBar' components/StatsPanel.tsx` returns a count >= 1
  - `grep -c 'innerRadius="30%"' components/StatsPanel.tsx` returns a count >= 1
  - `grep -c 'outerRadius="90%"' components/StatsPanel.tsx` returns a count >= 1
  - `grep -c 'startAngle={90}' components/StatsPanel.tsx` returns a count >= 1
  - `grep -c '风险项' components/StatsPanel.tsx` returns a count >= 1 (center overlay label preserved)
  - `grep -c 'PR 综合质量评级' components/StatsPanel.tsx` returns a count >= 1 (grade badge preserved)
  - `grep -v '^//' components/StatsPanel.tsx | grep -c 'radius = 35'` returns 0 (donut calc removed)
  - `grep -v '^//' components/StatsPanel.tsx | grep -c 'circumference'` returns 0 (donut calc removed)
  - `npx next build` compiles without errors
  </acceptance_criteria>

  <verify>
    <automated>npx next build</automated>
  </verify>

  <done>
  RadialBarChart renders 4 colored fan-shaped segments (blocker=red outermost, nit=gray innermost). Center shows risk count + "风险项". Hovering shows tooltip "{label}: {count} 项 ({percent}%)". Clicking a segment calls onFilterChange for severity filtering. Active filter dims non-matching segments. Grade badge column is untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace category horizontal bars with RadarChart</name>
  <files>components/StatsPanel.tsx</files>

  <read_first>
  - components/StatsPanel.tsx — read the current state after Task 1 modifications, especially the category bars section (right column) and existing `categoryDefs`/`catCounts` data
  </read_first>

  <action>
  **Step 2a: Extend Recharts imports**
  Add to the existing "recharts" import line: `RadarChart`, `Radar`, `PolarGrid`, `PolarAngleAxis`, `PolarRadiusAxis`, `Dot`.

  **Step 2b: Build radar data**
  Create a data array `radarData` from the existing `categoryDefs` and `catCounts`:
  - Each object has fields: `category` (label string like "安全漏洞"), `count` (number), `fullMark` (the `maxCat` value for uniform scale), `key` (category key like "security").
  - Map over `categoryDefs` to produce each data point.

  **Step 2c: Replace the category bars section (right column)**
  Keep the outer `<div>` wrapper with `rounded-lg border border-[#30363d] bg-[#161b22] p-6 shadow-sm` and the section header div (title "风险类别分布" + 清除筛选 button + zero-risk branch).

  For the `hasRisks` branch (replaces lines 186-213):
  - Render `<ResponsiveContainer width="100%" height={280}>` wrapping a `<RadarChart>`.
  - `<RadarChart>` props: `cx="50%" cy="45%" data={radarData}`.
  - Inside the RadarChart, render in this order:
    - `<PolarGrid stroke="#30363d" />`
    - `<PolarAngleAxis dataKey="category">` with a custom `tick` render function that renders `<text>` elements with `fill={isActive ? "#58a6ff" : "#8b949e"} fontSize={12} fontWeight={isActive ? 700 : 400}`. The custom tick determines `isActive` by finding the matching `categoryDefs` entry and checking `activeFilter?.type === "category" && activeFilter?.value === cat.key`. The `<text>` element has `cursor="pointer"` and `onClick` that calls `onFilterChange?.("category", cat.key)` or `onFilterChange?.("clear", null)` for toggle behavior.
    - `<PolarRadiusAxis tick={false} axisLine={false} />`
    - `<Radar dataKey="count" stroke="#58a6ff" fill="#58a6ff" fillOpacity={0.15} strokeWidth={2}>` with `dot` prop set to an object: `{ r: 4, fill: "#58a6ff", stroke: "#0d1117", cursor: "pointer" }`. Pass `activeDot` or `onClick` on the dot to implement toggle filtering — clicking a dot calls `onFilterChange?.("category", cat.key)` or `onFilterChange?.("clear", null)` if already active.
    - `<Tooltip formatter={(value) => [String(value) + " 项", ""]} />`

  - Remove the category bars JSX (the `categoryDefs.map` block that renders the horizontal colored bars with `h-1.5 w-full overflow-hidden rounded-full bg-[#21262d]` at lines 187-213).

  **Step 2d: Clean up unused variables**
  Verify `radius`, `circumference`, and `donutSegments` are removed from the file. The `maxCat` variable is still needed for `fullMark` in radarData. Keep `sevCounts` and `catCounts` — both are still used to build chart data.
  </action>

  <acceptance_criteria>
  - `grep -c 'RadarChart' components/StatsPanel.tsx` returns a count >= 1
  - `grep -c 'PolarGrid' components/StatsPanel.tsx` returns a count >= 1
  - `grep -c 'PolarAngleAxis' components/StatsPanel.tsx` returns a count >= 1
  - `grep -c 'PolarRadiusAxis' components/StatsPanel.tsx` returns a count >= 1
  - `grep -c 'dataKey="category"' components/StatsPanel.tsx` returns a count >= 1
  - `grep -c 'stroke="#58a6ff"' components/StatsPanel.tsx` returns a count >= 1
  - `grep -c 'fillOpacity={0.15}' components/StatsPanel.tsx` returns a count >= 1
  - `grep -c 'height={280}' components/StatsPanel.tsx` returns a count >= 1
  - `grep -c '风险类别分布' components/StatsPanel.tsx` returns a count >= 1 (section header preserved)
  - `grep -c 'h-1.5 w-full overflow-hidden rounded-full bg-\[#21262d\]' components/StatsPanel.tsx` returns 0 (category bars removed)
  - `npx next build` compiles without errors
  </acceptance_criteria>

  <verify>
    <automated>npx next build</automated>
  </verify>

  <done>
  RadarChart renders a blue polygon (#58a6ff) across 4 axes (安全漏洞/性能瓶颈/编码逻辑/代码规范). Each vertex is a clickable dot. Hovering shows tooltip "{count} 项". Clicking a vertex/axis label calls onFilterChange for category filtering. Clicking already-active item calls onFilterChange("clear"). Active filter highlights the matching axis label in blue. Section header and zero-risk branch are preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update stats-panel tests for Recharts rendering</name>
  <files>tests/stats-panel.test.tsx</files>

  <read_first>
  - tests/stats-panel.test.tsx — current test file with 4 tests: grade display, severity section, severity labels (Blocker/Major/Minor/Nit text), zero-risk state
  - tests/setup.ts — testing-library setup with jest-dom matchers and cleanup
  - components/StatsPanel.tsx — read the final state after Tasks 1 and 2 to understand what text content is available in the DOM
  </read_first>

  <behavior>
  - Test 1 (grade badge): should still pass as-is since the grade badge column is unchanged. Verify that "PR 综合质量评级" and grade letter "D" (from mockStats) are in the document.
  - Test 2 (section headers): verify both "风险严重度统计" and "风险类别分布" section headers render.
  - Test 3 (center overlay): the RadialBarChart center overlay div contains `stats.riskCount.toString()` (the number "5") and the text "风险项". Verify both are in the document.
  - Test 4 (zero-risk state): unchanged — verify "零高危隐患，代码状态极为优秀！" renders when riskCount is 0.
  - Test 5 (Recharts SVG containers): verify that at least 2 SVG elements with class `recharts-surface` exist in the document.
  - Remove tests that check for "Blocker", "Major", "Minor", "Nit" text content since these labels are now in Recharts tooltips (not in DOM by default).
  </behavior>

  <action>
  Rewrite `tests/stats-panel.test.tsx`:

  1. Keep the same imports (`describe`, `expect`, `test` from vitest; `render`, `screen` from @testing-library/react; `StatsPanel`, `StatsData` from @/components/StatsPanel) and the `mockStats` object.
  2. Keep the "显示综合质量评级" test (unchanged — checks "PR 综合质量评级" and grade letter "D").
  3. Replace the "显示风险严重度统计" test: check that `screen.getByText("风险严重度统计")` is in the document and that the center overlay displays `screen.getByText("5")` (the risk count from mockStats) and `screen.getByText("风险项")`.
  4. Remove the "显示所有严重级别分类" test entirely (it checked for "Blocker"/"Major"/"Minor"/"Nit" — these labels are now Recharts tooltip content, not DOM text that `screen.getByText` can find).
  5. Keep the "零风险时显示优秀状态" test (unchanged — check "零高危隐患，代码状态极为优秀！" with zeroStats).
  6. Add a test "显示风险类别分布标题": `expect(screen.getByText("风险类别分布")).toBeInTheDocument()`.
  7. Add a test "零风险时两个图表区均显示空态": render with `zeroStats`, then `expect(screen.getAllByText("零高危隐患，代码状态极为优秀！")).toHaveLength(2)`.
  8. Add a test "渲染 Recharts 图表容器": render with `mockStats`, then `expect(document.querySelectorAll(".recharts-surface").length).toBeGreaterThanOrEqual(2)`.
  </action>

  <acceptance_criteria>
  - `npx vitest run tests/stats-panel.test.tsx` passes all tests
  - `grep -v '^//' tests/stats-panel.test.tsx | grep -v '^#' | grep -c "Blocker\|Major\|Minor\|Nit"` returns 0 (English severity labels removed from test assertions)
  - The "显示综合质量评级" test still passes (grade badge unchanged)
  - The "零风险时显示优秀状态" test still passes (empty state unchanged)
  - New test "渲染 Recharts 图表容器" verifies at least 2 `.recharts-surface` SVG elements exist
  - New test "显示风险类别分布标题" verifies the section header for the RadarChart area
  - New test "零风险时两个图表区均显示空态" verifies both chart areas show the empty state
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run tests/stats-panel.test.tsx</automated>
  </verify>

  <done>
  All stats-panel tests pass against the new Recharts-based component. Tests verify grade badge, section headers, center overlay count, zero-risk state, and presence of Recharts-rendered SVG containers. No tests rely on removed text content (Blocker/Major/Minor/Nit labels).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → project | New recharts dependency installed via pnpm; supply chain trust boundary |
| StatsData → Tooltip render | Risk count and category data rendered inside Recharts Tooltip content formatter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | recharts npm package | mitigate | recharts is a widely-used vetted library (25k+ GitHub stars, 2M+ weekly downloads). Use pnpm lockfile for integrity verification. All installs go through `pnpm add recharts` which records the exact version hash in `pnpm-lock.yaml`. |
| T-05-02 | Information Disclosure | StatsPanel Tooltip formatter | accept | Tooltip displays severity/category labels and counts derived from StatsData — these are application-level aggregate statistics, not PII or secrets. No sensitive data exposed. |
| T-05-03 | Tampering | npm/pip/cargo installs | mitigate | slopcheck verification not required (recharts is a mainstream library). Package is treated as [ASSUMED] per fallback policy. Integrity guaranteed by pnpm lockfile hash. |
</threat_model>

<verification>
### Build verification
1. `npx next build` — compilation passes with zero errors
2. `npx vitest run` — all tests pass, no new failures

### Functional verification (manual via browser)
3. Launch dev server, submit a demo PR
4. Verify RadialBarChart renders 4 colored segments (blocker=red outermost, nit=gray innermost)
5. Verify center overlay shows risk count number + "风险项"
6. Click a radial segment → RiskCard list filters by that severity
7. Click the same segment again → filter clears
8. Verify RadarChart renders blue polygon across 4 axes
9. Click a radar vertex → RiskCard list filters by that category
10. Click the same vertex again → filter clears
11. Verify grade badge column is visually unchanged
12. Verify zero-risk state displays green empty state (not chart error)
13. Verify responsive layout at 375px / 768px / 1440px: 3-column grid collapses correctly
</verification>

<success_criteria>
- [ ] `pnpm ls recharts` shows recharts installed
- [ ] `npx next build` compiles with zero errors
- [ ] `npx vitest run` passes all tests (no new failures)
- [ ] RadialBarChart replaces the hand-rolled SVG donut
- [ ] RadarChart replaces the horizontal progress bars
- [ ] Center overlay displays risk count + "风险项"
- [ ] Filter click → callback fires; double-click → clear fires
- [ ] Zero-risk state shows green CheckCircle empty state
- [ ] Grade badge column is unchanged
- [ ] StatsData, FilterState types are unchanged
- [ ] onFilterChange interface signature is unchanged
</success_criteria>

<output>
Create `.planning/phases/05-chart-redesign/05-01-SUMMARY.md` when done
</output>
