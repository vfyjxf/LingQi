---
phase: 06-dimension-scoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/report/schema.ts
  - lib/config/default-config.ts
  - lingqi.config.json
  - tests/fixtures/report-fixtures.ts
  - tests/report-schema.test.ts
autonomous: true
requirements: [DIM-01, DIM-04]

must_haves:
  truths:
    - "DimensionScoreSchema validates: dimension (enum of 7), score (integer 0-100), severity (blocker/major/minor/nit), reasoning (non-empty string), evidence (non-empty string)"
    - "AiReviewReportSchema requires exactly 7 dimensionScores via z.array().length(7)"
    - "maxOutputTokens is 8000 in both default-config.ts and lingqi.config.json"
    - "tests/fixtures/report-fixtures.ts exports makeValidReport() returning a valid AiReviewReport with dimensionScores"
    - "Existing test file report-schema.test.ts passes with new dimension score validation tests"
  artifacts:
    - path: "lib/report/schema.ts"
      provides: "DimensionScoreSchema + AiReviewReportSchema extended with dimensionScores"
    - path: "lib/config/default-config.ts"
      provides: "maxOutputTokens: 8000"
    - path: "lingqi.config.json"
      provides: "maxOutputTokens: 8000"
    - path: "tests/fixtures/report-fixtures.ts"
      provides: "Shared makeValidReport() helper with default dimensionScores"
    - path: "tests/report-schema.test.ts"
      provides: "DimensionScoreSchema validation tests"
  key_links:
    - from: "DimensionScoreSchema"
      to: "AiReviewReportSchema.dimensionScores"
      via: "z.array(DimensionScoreSchema).length(7)"
    - from: "makeValidReport()"
      to: "all test files"
      via: "import from tests/fixtures/report-fixtures.ts"
---

<objective>
Define the DimensionScore Zod schema, extend AiReviewReportSchema to require exactly 7 dimension scores, double the AI output token budget from 4000 to 8000, and create a shared test fixture helper with dimension-level validation tests.

Purpose: Backend schema contract that the AI must produce (DIM-01: 0-100 per dimension) with mandatory evidence anchoring (DIM-04). The token increase prevents truncated JSON when the AI adds 7 dimension score objects to its output.
Output: DimensionScoreSchema, extended AiReviewReportSchema, updated config, shared fixture helper, and schema validation tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-dimension-scoring/06-RESEARCH.md
@lib/report/schema.ts
@lib/config/default-config.ts
@lingqi.config.json
@tests/report-schema.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define DimensionScoreSchema and extend AiReviewReportSchema</name>
  <files>lib/report/schema.ts</files>
  <read_first>
lib/report/schema.ts — current schema with RiskCategorySchema, SeveritySchema, AiReviewReportSchema, and parseAiReviewReport()
  </read_first>
  <behavior>
    - Test: AiReviewReportSchema.parse() accepts a valid report with exactly 7 dimensionScores, each having dimension/score/severity/reasoning/evidence
    - Test: AiReviewReportSchema.parse() rejects a report with 6 dimensionScores (z.array().length(7) violation)
    - Test: AiReviewReportSchema.parse() rejects a report with dimension score 101 (z.number().max(100) violation)
    - Test: AiReviewReportSchema.parse() rejects a dimension score of -1 (z.number().min(0) violation)
    - Test: AiReviewReportSchema.parse() rejects a dimension score with empty evidence string (z.string().min(1) violation)
    - Test: AiReviewReportSchema.parse() rejects a dimension score with invalid severity "critical" (not in SeveritySchema enum)
    - Test: DimensionScoreSchema.parse() accepts a non-integer score like 85.5 (if using .int(), reject with TypeError; research specifies .int())
  </behavior>
  <action>
Add to lib/report/schema.ts, after the existing RiskCategorySchema definition (line 20) and before RiskItemSchema (line 22):

1. Define DimensionScoreSchema as a z.object with these 5 fields, each using .describe() for AI guidance:
   - dimension: RiskCategorySchema — describe as "评分的维度: security/data/stability/performance/api/testing/maintainability"
   - score: z.number().int().min(0).max(100) — describe as "0-100 质量评分: 0=阻断性问题, 100=无风险"
   - severity: SeveritySchema — describe as "该维度最严重问题的级别: blocker/major/minor/nit"
   - reasoning: z.string().min(1) — describe as "先分析该维度发现的具体问题，再给出综合评分。这是推理过程。"
   - evidence: z.string().min(1) — describe as "引用具体文件名、行号和代码片段作为评分依据。不能是笼统描述。"

2. Export the DimensionScore type: `export type DimensionScore = z.infer<typeof DimensionScoreSchema>;`

3. Add to AiReviewReportSchema (inside the z.object({...}), after the existing groupAnalyses field at line 71, before contextNotes at line 72):
   dimensionScores: z.array(DimensionScoreSchema).length(7)
     .describe("7 个维度的评分数组: security/data/stability/performance/api/testing/maintainability 各一个"),

Per DIM-01: The schema enforces 0-100 integer scores for all 7 dimensions.
Per DIM-04: The evidence field with .min(1) enforces non-empty evidence anchoring.
  </action>
  <acceptance_criteria>
- AiReviewReportSchema.parse() succeeds with a report containing exactly 7 dimensionScores where each has valid dimension/score(0-100)/severity/reasoning/evidence
- AiReviewReportSchema.parse() throws ZodError when dimensionScores has 6 entries (length violation)
- AiReviewReportSchema.parse() throws when score=101 (max violation) or score=-1 (min violation)
- AiReviewReportSchema.parse() throws when evidence="" (min(1) violation)
- AiReviewReportSchema.parse() throws when severity="critical" (enum violation)
- npx tsc --noEmit passes on lib/report/schema.ts
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --reporter=verbose tests/report-schema.test.ts</automated>
  </verify>
  <done>
lib/report/schema.ts exports DimensionScoreSchema, DimensionScore type, and extended AiReviewReportSchema. Running `npx tsc --noEmit` produces no errors on the schema file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Increase maxOutputTokens from 4000 to 8000</name>
  <files>lib/config/default-config.ts, lingqi.config.json</files>
  <read_first>
lib/config/default-config.ts — find the ai.maxOutputTokens field at line 9
lingqi.config.json — find the ai.maxOutputTokens field at line 7
  </read_first>
  <action>
Change the value of maxOutputTokens from 4000 to 8000 in both files:

1. lib/config/default-config.ts — in the defaultLingQiConfig object, under the ai block: change `maxOutputTokens: 4000` to `maxOutputTokens: 8000`
2. lingqi.config.json — in the ai object: change `"maxOutputTokens": 4000` to `"maxOutputTokens": 8000`

This is required because the 7 dimension score objects (each with reasoning + evidence + score + severity + dimension fields) add approximately 700-1400 tokens to the AI output. The current 4000-token limit risks truncated JSON. 8000 is within the config schema bound of 16000.
  </action>
  <acceptance_criteria>
- grep 'maxOutputTokens.*8000' lib/config/default-config.ts returns exactly 1 match with value 8000
- grep 'maxOutputTokens.*8000' lingqi.config.json returns exactly 1 match with value 8000
- The grep command in verify returns 2 matching lines total
  </acceptance_criteria>
  <verify>
    <automated>grep -n 'maxOutputTokens.*8000' lib/config/default-config.ts lingqi.config.json</automated>
  </verify>
  <done>
Both files show maxOutputTokens: 8000. The grep command returns exactly 2 matching lines.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create shared test fixture helper + add schema validation tests</name>
  <files>tests/fixtures/report-fixtures.ts, tests/report-schema.test.ts</files>
  <read_first>
tests/report-schema.test.ts — existing validReport fixture (lines 7-83) and all test cases
lib/report/schema.ts — the extended schema after Task 1
  </read_first>
  <behavior>
    - Test: makeValidReport() returns an AiReviewReport that passes AiReviewReportSchema.parse()
    - Test: makeValidReport({ risks: [] }) overrides risks while keeping default dimensionScores
    - Test: AiReviewReportSchema validates dimensionScores array with exactly 7 entries
    - Test: AiReviewReportSchema rejects dimensionScores array with 6 entries
    - Test: AiReviewReportSchema rejects dimensionScores with score > 100
    - Test: AiReviewReportSchema rejects dimensionScores with score < 0
    - Test: AiReviewReportSchema rejects dimensionScores with empty evidence
    - Test: AiReviewReportSchema rejects dimensionScores with invalid severity
    - Test: All 8 pre-existing tests in report-schema.test.ts still pass (fixture updated with dimensionScores)
  </behavior>
  <action>
1. Create new file tests/fixtures/report-fixtures.ts:
   - Export a function `makeValidReport(overrides?: Partial<AiReviewReport>): AiReviewReport` that returns a default AiReviewReport object with:
     - The same summary/reviewFocus/risks/suggestions/groupAnalyses/contextNotes fields from the existing validReport in tests/report-schema.test.ts
     - A dimensionScores array of exactly 7 entries, one per dimension (security, data, stability, performance, api, testing, maintainability), each with:
       - score: 75
       - severity: "minor"
       - reasoning: "该维度未发现严重问题。"
       - evidence: "未在变更中发现该维度相关风险"
     - If overrides is provided, shallow-merge overrides on top of the default (use spread: { ...defaultReport, ...overrides })
   - Import AiReviewReport from "@/lib/report/schema"

2. Update tests/report-schema.test.ts:
   - Import makeValidReport from "@/../tests/fixtures/report-fixtures"
   - Replace the inline validReport constant with: `const validReport = makeValidReport();`
   - Add 6 new test cases after the existing tests:
     a. "接受包含 7 个维度评分的合法报告" — parse(makeValidReport()) succeeds
     b. "拒绝只有 6 个维度评分的报告" — parse(makeValidReport({ dimensionScores: makeValidReport().dimensionScores.slice(0, 6) })) throws
     c. "拒绝维度评分超出 0-100 范围" — clone and set score to 101 on one entry, parse throws
     d. "拒绝维度评分为负数" — clone and set score to -1 on one entry, parse throws
     e. "拒绝维度评分缺少证据" — clone and set evidence to "" on one entry, parse throws
     f. "拒绝非法的严重程度值" — clone and set severity to "critical" on one entry, parse throws
   - All 8 pre-existing test cases must still pass (they use validReport which now includes dimensionScores via makeValidReport())
  </action>
  <acceptance_criteria>
- tests/fixtures/report-fixtures.ts file exists with exported makeValidReport function
- makeValidReport() returns an object that passes AiReviewReportSchema.parse()
- makeValidReport({ risks: [] }) returns an object with empty risks but 7 dimensionScores
- tests/report-schema.test.ts has 14 test cases (8 original + 6 new), all passing
- npx vitest run --reporter=verbose tests/report-schema.test.ts exits with code 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --reporter=verbose tests/report-schema.test.ts</automated>
  </verify>
  <done>
tests/fixtures/report-fixtures.ts exists with makeValidReport(). tests/report-schema.test.ts has 14 tests (8 original + 6 new), all passing. No other test files are broken yet (they will be fixed in Plan 02 which depends on Plan 01).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI output -> Zod schema | Untrusted AI-generated JSON crosses into validation layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing | DimensionScoreSchema | mitigate | evidence field uses z.string().min(1) to reject fabricated/empty evidence; prompt enforces verbatim quoting |
| T-06-02 | Denial of Service | AiReviewReportSchema | mitigate | z.array(DimensionScoreSchema).length(7) fails parse if AI returns fewer than 7 dimensions, producing clear error |
| T-06-03 | Tampering | DimensionScoreSchema.score | mitigate | z.number().int().min(0).max(100) rejects out-of-range scores from prompt-injected bias |
| T-06-04 | Denial of Service | maxOutputTokens config | mitigate | Increased from 4000 to 8000 to prevent truncated JSON output for extended schema |
</threat_model>

<verification>
Run `npx vitest run --reporter=verbose tests/report-schema.test.ts` — all 14 tests must pass.

Run `npx tsc --noEmit` — no type errors on lib/report/schema.ts.
</verification>

<success_criteria>
- [ ] DimensionScoreSchema defined in lib/report/schema.ts with 5 fields (dimension, score, severity, reasoning, evidence)
- [ ] AiReviewReportSchema includes dimensionScores: z.array(DimensionScoreSchema).length(7)
- [ ] DimensionScore type exported
- [ ] maxOutputTokens: 8000 in both lib/config/default-config.ts and lingqi.config.json
- [ ] tests/fixtures/report-fixtures.ts exports makeValidReport() with default dimensionScores
- [ ] tests/report-schema.test.ts has 14 passing tests including 6 new dimension validation tests
</success_criteria>

<output>
Create `.planning/phases/06-dimension-scoring/06-01-SUMMARY.md` when done
</output>
